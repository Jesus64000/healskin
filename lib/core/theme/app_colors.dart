import 'package:flutter/material.dart';

class AppColors {
  // Fondos y Superficies (Modern Slate)
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color surfaceDark = Color(0xFF1E293B);
  static const Color surfaceLight = Color(0xFFFFFFFF);

  // Marca y Acentos (HealSkin)
  static const Color primary = Color(0xFF2563EB); // Azul Clínico
  static const Color secondary = Color(0xFF00D2FF); // Cian IA
  static const Color accentSkin = Color(0xFFFFB6B9); // Tono Piel (Calidez)

  // Estados
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444); // Alertas de la IA

  // UI Glassmorphism
  static const Color glassOverlay = Color(0x1AFFFFFF);
  static const Color textPrimaryLight = Color(0xFF0F172A);
  static const Color textPrimaryDark = Color(0xFFF8FAFC);
}