import 'package:flutter/material.dart';

class AppColors {
  // Fondos y Superficies (Light Mode - Skincare Aesthetic)
  static const Color backgroundLight = Color(0xFFFFFFFF); // Blanco puro
  static const Color surfaceLight = Color(0xFFF8F9FA); // Gris muy claro para tarjetas
  static const Color surfaceDark = Color(0xFFFFF0F0); // Tono durazno ultra claro para fondos de tarjetas

  // Marca y Acentos (HealSkin - Extraídos del PDF del cliente)
  static const Color primary = Color(0xFFE8A09A); // Salmón / Durazno (Botones, iconos activos)
  static const Color secondary = Color(0xFFB5B2E5); // Lavanda / Morado Suave (Banners, botones secundarios)
  static const Color accentDark = Color(0xFFC76D74); // Rosa/Rojo oscuro (Fondo de la pantalla de cuestionario)

  // Textos
  static const Color textPrimary = Color(0xFF2D3142); // Gris oscuro azulado para títulos
  static const Color textSecondary = Color(0xFF9094A6); // Gris suave para subtítulos

  // Estados
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);
}